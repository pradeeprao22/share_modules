$('#follow-user-<%= @user.id.to_s %>').
  html('<%= link_to "Follow", follow_path(@follow), method: :delete, remote: true, class: "btn btn-outline-dark common-btn edit-profile-btn" %>');
  console.log("create follow success");